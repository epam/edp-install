{{- if .Values.sso.enabled -}}
{{- if .Values.argocd.enabled -}}
apiVersion: v1.edp.epam.com/v1
kind: KeycloakRealmGroup
metadata:
  name: argocd-devs
spec:
  name: Developer
  realm: main
{{- end }}
{{ end }}
