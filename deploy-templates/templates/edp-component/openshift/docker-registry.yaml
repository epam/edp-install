{{- if eq .Values.global.platform "openshift" -}}
apiVersion: v1.edp.epam.com/v1
kind: EDPComponent
metadata:
  name: docker-registry
spec:
  type: docker-registry
  url: {{ .Values.global.dockerRegistry.url }}
  visible: false
  icon: stub-value
{{end}}
