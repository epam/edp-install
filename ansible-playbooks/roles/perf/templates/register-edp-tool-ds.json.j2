{
  "config": {
    "url": "{{ url }}",
    "username": "{{ user }}",
    "password": "{{ password }}",
    "instanceId": "{{ url }}"
  },
  "name": "{{ tool | upper }}",
  "type": "{{ tool | upper }}"
}